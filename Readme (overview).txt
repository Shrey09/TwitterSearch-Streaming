
All the images of the output and steps required to setup cloud cluster, creating twitter developed account, mongodb database and apache spark (program and its output) are stored in screenshot folder.

Apache spark program is in MapReduce.py
search - twitter_search.py
stream- twitter_stream.py

Stream is done 2 times and tweets are stored in two stream csv files.
Search is carried out once.
All the cleaned tweets are stored in mongodb database in cloud cluster.
Mapreduce is done on streamed tweets collected in both the files.

Report is created to show the steps and explain the process carried out in the assignment.

All the references used in the assignment for undesrtanding the concepts and code are mentioned in my report.